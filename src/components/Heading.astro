---
interface Props {
    level: 1 | 2 | 3
}
const { level } = Astro.props;
---

{level === 1 && <h1 class="text-5xl font-extrabold"><slot /></h1>}
{level === 2 && <h2 class="text-2xl border-b pb-1 font-bold"><slot /></h2>}
{level === 3 && <h3 class="text-lg font-semibold"><slot /></h3>}
